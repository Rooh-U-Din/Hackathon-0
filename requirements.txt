# Python standard library (no installation needed)
# Already included: sys, os, pathlib, datetime, json, time, subprocess

# Core dependencies
schedule>=1.1.0

# Gmail API
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# Browser automation (WhatsApp, LinkedIn)
playwright>=1.40.0

# Optional but recommended
python-dotenv>=1.0.0  # For .env file support
watchdog>=3.0.0       # For file system watching
requests>=2.31.0      # For API calls

# Development dependencies (optional)
pytest>=7.4.0         # For testing
black>=23.0.0         # For code formatting
pylint>=3.0.0         # For linting
